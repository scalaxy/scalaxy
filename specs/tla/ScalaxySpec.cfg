SPECIFICATION Spec
CONSTANTS
  Nodes = {"n1", "n2", "n3"}
  Keys = {"k1", "k2"}
  Rf = 2
  ZoneOneNodes = {"n1", "n2"}
  ZoneTwoNodes = {"n3"}INVARIANT
  TypeOK
  DataIntegrityUnlessMediaLoss
  NoFalseLossAlarm
  NoUndetectedLoss
  DeleteVisible
  EncryptionAtRest
  ZoneSpreadReplication
  AvailabilityUnderSingleZoneFailure
  AvailabilityUnderSingleFailureAfterReplication
PROPERTY
  TopologyHeals
  ServiceRestored
  ReplicationConverges
CHECK_DEADLOCK FALSE
\* Justification: closed model over a fixed key set. Once every key is
\* tombstoned no action is enabled -- legitimate quiescence, not a fault.
